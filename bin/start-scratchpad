#!/bin/bash

file=$HOME/NOTES

# Start the vim in a terminal (without swap file, assume we save all the time manually anyway)
if [ -z $(pgrep -a vim | grep $file) ]; then
    $TERMINAL --title="Notes :: vim" -e "vim -n $file"
else
    i3-msg '[title="^Notes ::"] scratchpad show'
fi

